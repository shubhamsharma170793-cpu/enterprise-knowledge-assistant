# ----------------------------------------
# Core framework
# ----------------------------------------
streamlit==1.39.0        # Web UI framework

# ----------------------------------------
# PDF processing
# ----------------------------------------
pymupdf==1.22.5          # For extracting text from PDFs (fitz)

# ----------------------------------------
# Embeddings & NLP
# ----------------------------------------
langchain==0.2.12            # Provides text splitters, utilities
sentence-transformers==3.0.1 # Embeddings (all-MiniLM-L6-v2)

# ----------------------------------------
# Vector search
# ----------------------------------------
faiss-cpu==1.9.0         # Vector database for semantic search
numpy                    # Needed for FAISS and math ops

# ----------------------------------------
# Hugging Face models
# ----------------------------------------
transformers==4.44.2     # Hugging Face library for LLM pipelines
torch==2.2.2+cpu         # CPU-only PyTorch (lighter, avoids GPU issues)

# Hugging Face Transformers + PyTorch
transformers==4.48.3
torch==2.8.0
